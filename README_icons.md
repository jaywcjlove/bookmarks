# 📚 webloc 文件图标设置工具

这个工具可以自动为 `.webloc` 文件设置图标，根据网址的域名从 `icons` 目录中匹配对应的图标文件。

## 🚀 使用方法

### 方法1: 直接运行 Swift 脚本 (需要安装 Swift)
```bash
swift set_icons.swift
```

### 方法2: 编译成二进制文件 (推荐)
```bash
# 编译
./build.sh

# 或者手动编译
swiftc -o set_icons set_icons.swift

# 运行
./set_icons
```

## 📁 目录结构

```
项目根目录/
├── bookmarks/          # 存放 .webloc 文件
│   ├── GitHub.webloc
│   ├── Gmail.webloc
│   └── ...
├── icons/              # 存放图标文件
│   ├── github.com.icns
│   ├── google.com.icns
│   └── ...
├── set_icons.swift     # Swift 源码
├── build.sh           # 编译脚本
└── set_icons          # 编译后的二进制文件
```

## 🎯 工作原理

1. **解析 webloc 文件**: 读取 `.webloc` 文件中的 XML，提取 URL
2. **提取域名**: 从 URL 中提取域名，并处理特殊情况：
   - `www.reddit.com` → `reddit.com`
   - `mail.google.com` → `mail.google.com`
3. **匹配图标**: 在 `icons` 目录中查找对应的图标文件
   - 优先匹配 `.icns` 格式
   - 其次匹配 `.png` 格式
4. **设置图标**: 使用 macOS 系统 API 设置文件图标

## 📝 图标命名规则

图标文件应该以域名命名，例如：
- `github.com.icns` 对应 `https://github.com/`
- `google.com.icns` 对应 `https://www.google.com/`
- `reddit.com.icns` 对应 `https://www.reddit.com/`

## 🔧 关于 Swift 依赖

### Swift 是否默认安装？
- **不是**。Swift 编译器默认不会安装在 macOS 上
- 需要安装 Xcode 或 Xcode Command Line Tools 才有 Swift 编译器

### 解决方案
1. **编译成二进制文件** (推荐)：
   - 在有 Swift 的机器上编译：`swiftc -o set_icons set_icons.swift`
   - 编译后的二进制文件可以在任何 macOS 系统上运行
   - 无需安装任何开发工具

2. **安装 Swift 环境**：
   ```bash
   # 安装 Xcode Command Line Tools
   xcode-select --install
   ```

## ✨ 特点

- ✅ 无需第三方依赖
- ✅ 使用 macOS 原生 API
- ✅ 支持 .icns 和 .png 格式图标
- ✅ 智能域名匹配
- ✅ 详细的运行日志
- ✅ 可编译成独立二进制文件

## 🔍 示例输出

```
📁 项目目录: /Users/user/bookmarks
📁 Bookmarks目录: /Users/user/bookmarks/bookmarks
📁 Icons目录: /Users/user/bookmarks/icons

🚀 开始设置 webloc 文件图标...

🔗 GitHub.webloc -> https://github.com/
🌐 域名: github.com
🔧 使用图标: github.com.icns
✅ 成功为 GitHub.webloc 设置图标

🎉 完成! 成功设置 1/1 个文件的图标
```
